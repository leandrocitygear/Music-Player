name: Build Disc-Out

on:
  push:
    branches:
      - master

  workflow_dispatch:

jobs:
  build-windows:
    name: Build Windows Squirrel Installer
    runs-on: windows-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v5

    - name: Setup Node
      uses: actions/setup-node@v5
      with:
        node-version: 22
        cache: npm

    - name: Install dependencies
      run: npm ci

    - name: Rebuild native modules for Electron
      run: npx electron-rebuild -f -w better-sqlite3

    - name: Build Squirrel installer
      run: npm run make -- --platform=win32 --arch=x64
      env:
        DEBUG: electron-forge:*

    - name: Upload Windows installer
      uses: actions/upload-artifact@v4
      with:
        name: disc-out-windows
        path: |
          out/make/squirrel.windows/x64/*.exe
          out/make/squirrel.windows/x64/*.nupkg
          out/make/squirrel.windows/x64/RELEASES
      
  # build-macos-intel:
  #   name: Build macOS Intel Installer
  #   runs-on: macos-latest

  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v5

  #     - name: Setup Node
  #       uses: actions/setup-node@v5
  #       with:
  #         node-version: 22
  #         cache: npm

  #     - name: Setup Python
  #       uses: actions/setup-python@v6
  #       with:
  #         python-version: "3.12"

  #     - name: Set Python
  #       shell: bash
  #       run: |
  #         echo "PYTHON=$pythonLocation/python" >> "$GITHUB_ENV"

  #     - name: Install dependencies
  #       run: npm ci --ignore-scripts

  #     # Electron 38+ requires macOS 12.
  #     # Electron 37 still supports macOS 11 Big Sur.
  #     - name: Install Big Sur compatible Electron
  #       run: |
  #         npm install electron@37.3.1 --save-exact

  #     - name: Rebuild better-sqlite3
  #       run: npx electron-rebuild -f -w better-sqlite3

  #     - name: Rebuild DMG native dependencies
  #       run: |
  #         npm rebuild fs-xattr
  #         npm rebuild macos-alias
  #         npm rebuild ds-store

  #     - name: Verify native modules
  #       run: |
  #         ls -la node_modules/better-sqlite3/build/Release/
  #         ls -la node_modules/fs-xattr/build/Release/
  #         ls -la node_modules/macos-alias/build/Release/ || true
  #         ls -la node_modules/ds-store/build/Release/ || true

  #     - name: Verify Electron version
  #       run: |
  #         npx electron --version

  #     - name: Build macOS Intel installer
  #       run: npm run make -- --arch=x64

  #     - name: Upload macOS Intel installer
  #       uses: actions/upload-artifact@v5
  #       with:
  #         name: Disc-Out-macOS-Intel-Big-Sur
  #         path: out/make/**
  #         if-no-files-found: error

  # build-linux:
  #   name: Build Linux Installers
  #   runs-on: ubuntu-latest

  #   steps:
  #   - name: Checkout
  #     uses: actions/checkout@v5

  #   - name: Setup Node
  #     uses: actions/setup-node@v5
  #     with:
  #       node-version: 22
  #       cache: npm

  #   - name: Setup Python
  #     uses: actions/setup-python@v6
  #     with:
  #       python-version: "3.12"

  #   - name: Set Python
  #     shell: bash
  #     run: |
  #       echo "PYTHON=$pythonLocation/python" >> "$GITHUB_ENV"

  #   - name: Install dependencies
  #     run: npm ci --ignore-scripts

  #   - name: Rebuild better-sqlite3
  #     run: npx electron-rebuild -f -w better-sqlite3

  #   - name: Build Linux installers
  #     run: npm run make

  #   - name: Upload Linux installers
  #     uses: actions/upload-artifact@v5
  #     with:
  #       name: Disc-Out-Linux-Installers
  #       path: out/make/**
  #       if-no-files-found: error